# --- Auth ---
# Generate with: openssl rand -base64 32
AUTH_SECRET=
# Only needed if your Vercel preview URLs differ from NEXTAUTH_URL fallback.
# NEXTAUTH_URL=https://your-vercel-domain.vercel.app

# --- Google OAuth (https://console.cloud.google.com) ---
AUTH_GOOGLE_ID=your-client-id.apps.googleusercontent.com
AUTH_GOOGLE_SECRET=your-client-secret

# --- Postgres (Vercel Postgres / Neon auto-injects these when you attach the integration) ---
DATABASE_URL=postgres://user:pass@host/db?pgbouncer=true&connection_limit=1
DIRECT_URL=postgres://user:pass@host/db

# --- Cron auth (set any random string; paste the same into Vercel env) ---
CRON_SECRET=
