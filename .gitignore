node_modules/
.next/
out/
tsconfig.tsbuildinfo
.env
.env.local
.env.production

npm-debug.log*
pnpm-debug.log*
.DS_Store

# prisma
prisma/*.db

# data dumps (legacy)
data/*.json

# local tooling scratch space — must never be committed
# .claude/settings.local.json is per-user permission config
# .superpowers/ holds brainstorm/sketch session state
# graphify-out/ is the knowledge-graph build output
.claude/settings.local.json
.superpowers/
graphify-out/
